const result = require('./exercise');

describe('Exercise 19 - Find an element\'s position in an array', () => {
    test('The result should be the position of Mario in the array', () => {
        expect(result).toBe(3);
    });
});
